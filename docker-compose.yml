services:
  vm:
    build:
      context: /home/miko/projects/utils/vm
      dockerfile: providers/docker/Dockerfile
      args:
        PROJECT_USER: "developer"
        PROJECT_UID: "1000"
        PROJECT_GID: "1000"
    container_name: vm-dev
    hostname: dev.vm.local
    tty: true
    stdin_open: true
    environment:
      - LANG=en_US.UTF-8
      - LC_ALL=en_US.UTF-8
      - TZ=America/Los_Angeles
      - PROJECT_USER=developer
      - PULSE_SERVER=unix:/run/user/1000/pulse/native
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=all
    volumes:
      - .:/workspace:delegated
      - /home/miko/projects/utils/vm:/vm-tool:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - vm_nvm:/home/developer/.nvm
      - vm_cache:/home/developer/.cache
      - vm_config:/tmp
      - /home/miko/.claude/vms/vm:/home/developer/.claude:delegated
      - /home/miko/.gemini/vms/vm:/home/developer/.gemini:delegated
      - /run/user/1000/pulse:/run/user/1000/pulse
    devices:
      - /dev/snd:/dev/snd
      - /dev/dri:/dev/dri
    group_add:
      - audio
      - video
      - render
    networks:
      - vm_network
    cap_add:
      - SYS_PTRACE
    security_opt:
      - seccomp:unconfined

networks:
  vm_network:
    driver: bridge

volumes:
  vm_nvm:
  vm_cache:
  vm_config:
